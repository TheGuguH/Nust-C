namespace std::io

use std::string

def Print(string text){
    $__PRINT_A_STRING_(text)
}

def PrintF(string format, ... any[] values){
    string textToPrint = FormatString(format, values);

    Print(textToPrint);
}